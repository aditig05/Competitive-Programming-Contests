#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    while (t--) {
    int n, m;
    cin >> n >> m;
    int mn = max(n, m);
    long long max_sum = mn +1;
    cout << max_sum << endl;
    }
}
